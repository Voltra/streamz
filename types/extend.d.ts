/**
 * Helper function that installs global extensions
 * @param ctx - The context on which it will be installed (e.g. window, global, self, this, etc...)
 */
export declare const extend: (ctx: typeof globalThis) => void;
